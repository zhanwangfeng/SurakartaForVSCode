# Changelog

## 0.3.0

- 新增局域网联机对战（1v1），复用 `src/common` 联机模块。
- TreeView 新增「联机对战」分组：创建房间、加入房间、设定昵称、音效开关。
- 联机界面：移除模式 / 难度等无用控件；对手加入后由房主点击「开始对战」才显示棋盘；己方棋子始终在下方（双方棋盘互为镜像）；棋盘上下显示轮到该方行动的图标。
- 对局状态（开始 / 重新开始）只能由房主操作，客机相应按钮禁用。
- 联机棋盘上方状态：对局中显示「轮到你行棋 / 等待对手行棋」，结束后双方对称显示「对局结束：橙方（昵称） 获胜，你赢了 / 你输了」。
- 新增设置项 `surakarta.multiPort`（默认 18766）。
- 构建：按 F5 调试时不再运行 `watch`，改为执行一次 `npm run compile`。

## 0.1.0

- 初始版本：活动栏入口、TreeView 对战模式菜单、Webview 游戏主体。
- 支持双人对战与三档难度的人机对战。
